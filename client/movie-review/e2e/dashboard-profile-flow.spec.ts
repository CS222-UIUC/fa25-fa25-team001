import { test } from '@playwright/test';

test.describe('Dashboard and Profile Flow', () => {
  test.beforeEach(async () => {
    // Add something here if needed
  });

  test('test 1', async ({ page }) => {
    // Add something here
    await page.goto('/');
  });

  test('test 2', async ({ page }) => {
    // Add something here
    await page.goto('/');
  });

  test('test 3', async ({ page }) => {
    // Add something here
    await page.goto('/');
  });

  test('test 4', async ({ page }) => {
    // Add something here
    await page.goto('/');
  });

  test('test 5', async ({ page }) => {
    // Add something here
    await page.goto('/');
  });
});
